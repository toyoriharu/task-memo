{
  "name": "task-memo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager . TaskMemo --platform=win32 --arch=x64 --electronVersion=4.0.0 --overwrite --asar --icon=icon.ico --version-string.FileDescription=TaskMemo"
  },
  "keywords": [],
  "author": "toyoriharu",
  "license": "ISC",
  "dependencies": {
    "vue": "^2.5.21"
  },
  "devDependencies": {
    "electron": "^4.0.0",
    "electron-packager": "^13.0.1"
  }
}
